---
title: Session Handoff
tags: [cimp, handoff, resume]
updated: 2026-07-28
---
# Session Handoff — read this first when resuming
← [[CIMP - Home]]

> Purpose: pick up work in a **fresh session** without re-deriving context. Read this + [[Architecture Overview]] + [[Backend Modules and API]] and you're oriented.

## Where the code is
- **Active branch: `dev`** — ~42 commits ahead of **`main`** (the deploy branch). All recent work (security + features + the UI/UX batch) is on `dev`. `main` is untouched.
- Repo `D:\CIMP\CIMP` (GitHub `RagnarokAnsh/CIMP`). Vault `D:\CIMP\CIMP\vault`.
- Sibling repos: `D:\cimp-connect` ([[cimp-connect Package]]), `D:\FAFICS` ([[FAFICS Integration]]).

## State (green)
- **262 unit (32 suites) + 40 e2e (backend) + 13 Playwright pass**; both typechecks clean; **ESLint green** (`npm run lint` in each root — 0 errors; 86 backend + 8 frontend warnings, informational); frontend build clean. Verify: `npm test && npm run test:e2e` (root) and `cd frontend && npm run test:e2e` (see the Playwright recipe under dev gotchas).
- ✅ **The Playwright suite ran 2026-07-28 and is GREEN — 13 passed, 1 skipped, 0 failed (29.5s).** It is **14 tests** (13 executed + 1 `test.skip`), not the 12 previously recorded. The 2026-07-27 UI/UX batch's interaction changes (board card → link, Triage priority → group, second login button) did **not** break it. It had never run for two *environment* reasons, both now known: **Playwright's browsers were never installed**, and **the documented Postgres port is wrong** — see dev gotchas.
- 🚀 **2026-07-29: SHIPPED TO PRODUCTION.** `dev` merged to `main`, deployed twice, schema **11 → 20** (DB snapshot taken first). Two config blockers cleared: `SSH_KNOWN_HOSTS` was unset *and* main's workflow had no host-key guard (it lives only on dev, so the merge introduced it and the deploy that used it); and `RUN_MIGRATIONS` was a **secret** while the workflow reads `vars.`, so migrations would have silently skipped. Also shipped: **Reports folded into Dashboard** behind a platform selector (they shared 7 of 8 KPI cards — `/staff/reports` now redirects), and **`ecosystem.config.cjs`** bounding pm2 restarts. Live: `https://35.154.196.105`. → Changelog (2026-07-29) · [[Deployment, CI-CD and Dev Workflow]]
- ℹ️ **`cimp-api`'s ~21k restart count is historical, not a live fault.** 20,975 of them are the 2026-07-07/08 crash loop (fail-closed `SCAN_DRIVER` vs a stale `.env`); every day since is 1–4 deploys. Read `unstable_restarts` + `uptime`, not `restart_time`. The co-hosted `whoop` apps bounce every ~28 min on their own watchdog — **not** caused by our deploys, and not ours to fix.
- ✅ **2026-07-28 (3): second audit pass (5 agents, all completed) — 25 findings, zero critical/high, ALL FIXED.** The IDOR sweep covered ~80 routes across **31** controllers (not 24 — `deflection.controllers.ts` is plural and the `*.controller.ts` glob had always missed it) and found **one low** issue: the access-control layer holds. Biggest catches: **staff emails were enumerable by login timing** (the anti-timing dummy hash was 59 chars, so bcryptjs short-circuited — measured 0.00 ms vs 83 ms, a binary oracle, now fixed with a valid hash + spec); **SSE had no connection cap and ignored `tokenVersion`**, so a password reset stopped nothing already connected; the **last 403/404 existence oracle** (staff attachment download); **partial upload failures orphaned blobs**; **pagination had no unique tiebreak** so paging a status-sorted list repeated and skipped rows; failed queries **rendered as empty states** on four admin cards and the bell. Also verified against TypeORM's source: the "bump updatedAt without touching the version" comment was **backwards** — `UpdateQueryBuilder` always increments a `@VersionColumn`, so any comment on an issue bumps its version (behaviour kept, comments corrected). → Changelog (2026-07-28 (3))
- ✅ **2026-07-28 (2): all audit findings FIXED, plus three more.** The Critical is closed — but **not** the way the entry below suggested: a `Promise.race` timeout cannot rescue a *synchronous* hang (the PoC's heartbeat never fires), so `src/common/magic-bytes.ts` now does the signature check and **`file-type` is uninstalled**. Also fixed: the **mentions picker** (a `.slice(0,6)` cap hid 5 of 11 staff; mentions typed by hand rendered highlighted but notified nobody), a **pre-existing bug where `/staff/me` and `/staff/notifications` were sent with no `Authorization` header** (two Vite module instances → two `staffTokenGetter` copies; `client.ts` now falls back to `sessionStorage`), `SERVABLE_SCAN` consolidation, the three fail-open env flags, reporter cache clearing on token swap, 12 filter labels, the DB port, `DESTRUCTIVE_ICON`, and the stale JQL comment. **257/32 unit + 40 e2e + 13 Playwright green.** ⚠️ **Known suite fragility:** `staffLogin` runs per test against a 10/min login throttle — with Playwright retries a run can exceed it and login times out, looking like an app failure. Fix with a shared `storageState` **before wiring CI**. → Changelog (2026-07-28 (2))
- 🔴 **2026-07-28: audit pass — one Critical found (now fixed, see above).** No code changed; findings are open and are the next session's work. **`file-type` 16.5.4 (`src/reporter/reporter.service.ts:64`) hangs the event loop forever on a 118-byte crafted ASF upload — verified with a working PoC.** Single-threaded Node + no request timeout = one reporter request takes the whole API down for every tenant. The MIME allowlist does not help: the parse that hangs runs *before* the allowlist check on line 65. Mitigate with a `Promise.race` timeout around the sniff; real fix is `file-type` 22.x (major, ESM). Also open: dependency advisories are now **31 backend / 12 frontend** and several are *runtime* (`nodemailer` SMTP/CRLF injection, `multer` DoS, `react-router` open-redirect→XSS) — the old "18/7, mostly build-time" note was stale; the servable-scan constant is defined **3×**; the reporter query cache is not cleared on hand-off token swap; filter/search inputs are placeholder-labelled only (WCAG AA); `SLA_SWEEP_ENABLED`/`DIGEST_ENABLED`/`SCAN_RETRY_ENABLED` **fail open** on a typo and bypass `env.validation.ts`. Full list + the verified-clean results → Changelog (2026-07-28) · [[Security Audit and Hardening]]
- **2026-07-27 (2): UI/UX audit — 48 findings measured and fixed, 6 commits.** Frontend only; **no backend, API or schema change, no migration.** Colour tokens re-solved against WCAG (light mode was systematically under-checked), status dots re-solved for colour-vision distance, the type scale actually implemented, heading semantics restored, focus ring / kbd / z-index / meter tones consolidated, accessibility floor established (skip link, route titles, landmarks), reporter i18n completed. `DESIGN.md` rewritten to match what ships. → Changelog · [[Frontend Overview]] · `DESIGN.md`
- **2026-07-27 (1): audit pass + six-feature batch.** A full audit (OWASP, bugs, redundancy, reuse, QA, UI/UX) found **no exploitable holes and no functional bugs**; then shipped: 429/error message mapping, **canned responses**, **tenant-owner reporting**, **public status page**, **reporter i18n (EN/ES/FR/DE)**, and **machine translation** of reporter↔staff messages. **Migrations now #20** (18 canned responses, 19 status page, 20 comment translations). New env (all default off/none): `TRANSLATE_DRIVER`, `TRANSLATE_API_URL`, `TRANSLATE_API_KEY`, `TRANSLATE_STAFF_LOCALE`, `TRANSLATE_REPORTER_LOCALES`. → Changelog · [[Module - Status Page]] · [[Module - Translation]] · [[Module - Canned Responses]]
- **Open from those audits (not yet done):** a status-machine parity test between `src/issues/status-machine.ts` and `frontend/src/lib/issue-status.ts` — **re-verified 2026-07-28 as still in agreement** (the frontend's extra `from === to` guard is redundant, not divergent), but nothing enforces it. ✅ The duplicated audit-action label formatting is **done** — `actionLabel` in `lib/issue-meta.ts` now serves both `AuditPage` and `IssueDetailPanel`. ⚠️ The dependency line that used to sit here ("18 / 7, mostly build-time tooling") was **measured wrong and is superseded** by the 2026-07-28 numbers above — do not act on it.
- **Design-system rules are now enforceable, so respect them:** no raw Tailwind palette outside `issue-meta.ts`/`status-meta.ts` (currently zero violations), no arbitrary type sizes (11px floor has a token), one `.focus-ring`, `--input` ≠ `--border`. `DESIGN.md` is accurate as of this batch — it previously described a system that had never been built. → [[Frontend Overview]]
- **2026-07-24: audit remediation batch** — essentially all of a ~30-finding audit pass fixed (tenant-isolation oracles, live SSE/notification/token revocation, webhook redirect SSRF, automation validation+events, Jira side-effects, scan-retry sweep, storage-cleanup, trust-proxy, plus consistency/dedup + the ESLint gate). Migrations unchanged (still #17). New env (all default on): `SCAN_RETRY_ENABLED`; new opt-in: `TRUST_PROXY` (default off). → Changelog · [[Security Audit and Hardening]]
- **2026-07-22: admin CRUD completed** — platform + staff lifecycle (disable/enable/delete), self- and last-admin lockout guards, a ghost-account fix in `upsertFromClaims`, and confirmation dialogs on every destructive action. No migration needed (`status` columns already existed). → Changelog · [[Module - Admin]]
- **2026-07-13 audit pass done** (auth hardening + dedup — see Changelog). Top remaining engineering gaps, in order: **CI pipeline** (231 tests, nothing runs them automatically), error monitoring, then deploy dev→main (migrations 12-17) + domain/TLS.
- **Differentiator track COMPLETE except Plan 05 (AI triage — user deferred):** merge (02), webhooks (01), SDK context capture (03, v0.5.0), CSAT (06), deflection (04, v0.6.0), triage inbox (07). PLUS 2026-07-12: **SLA policies/escalations (L8 fixed), Admin Integrations UI, ops analytics + weekly digest, screenshots (cimp-connect v0.7.0)**. All live-verified. Migrations now #17 (`AddSlaPolicy`). New deps: `@nestjs/schedule`.
- **Deploy note:** dev is many features ahead of the AWS `main` deploy — merging dev→main runs migrations 12-17 and ships all new surfaces at once. New env (optional): `SLA_SWEEP_ENABLED`, `DIGEST_ENABLED` (both default on).
- **Local dev ports:** FAFICS now squats :3000 (web) and :3001 (api) — run the CIMP backend with `PORT=<free> npm run start:dev` when both are up.
- **Security:** the 2026-07-24 batch closed essentially all remaining open findings from the audit (tenant-isolation oracles, live revocation across SSE/notifications/tokens, webhook redirect SSRF, +more). Tracker: `SECURITY_AUDIT.md`. → [[Security Audit and Hardening]].
- **Features shipped:** issue links, labels, watchers, automation rules, scoped API tokens (all backend+tests+**UI** — automation/token UIs now live in Admin → Integrations); board WIP limits **+ swimlanes** (UI); SSE-ticket auth; **WATCHER read-only role** (backend+tests+UI, admin-managed, per-platform or global). Plus the differentiator track: merge, webhooks, SDK context capture, CSAT, deflection, triage inbox, SLA policies/escalations, ops analytics/digest, JQL. Plus the 2026-07-27 batch: **canned responses, tenant-owner reporting, public status page, reporter i18n, machine translation**. → [[Features - Shipped]] / [[Module - Authz]].

## What's next (priority order) → [[Feature Roadmap]]
The differentiator + JIRA-like feature tracks are **done** (SLA policies/escalations, automation/API-token UIs, board swimlanes, JQL all shipped — see [[Features - Shipped]]). Remaining, in order:
0. ✅ **Done — the 2026-07-28 audit findings are all fixed** (see the entry above). What remains from that pass: give the e2e suite a shared `storageState` so it stops riding the 10/min login throttle, and add the status-machine parity test.
1. **CI pipeline — the "nothing runs automatically" claim here was WRONG.** `.github/workflows/deploy.yml` has existed since 2026-07-22 and gates every push to `main` on `npm ci && typecheck && test && test:e2e && build` (backend) plus `typecheck && build` (frontend), then deploys to EC2 over SSH. The real gaps are narrower and worth fixing in this order: **(a)** nothing runs on `dev` or on PRs, so `main` is the first place a break is caught — the gate is a deploy gate, not a development one; **(b)** the gate omits **Playwright** and **`npm run lint`**, so the 13 browser tests and the lint floor are unenforced (CI would need `npx playwright install --with-deps chromium` and a Postgres service); **(c)** the Playwright suite would flake in CI anyway until it stops logging in per test — see the 10/min throttle note above. Fix (c) before adding (b).
2. **`npm audit`** in both roots — now **31 backend (10 high) / 12 frontend (9 high)**, and *not* mostly build-time: `nodemailer`, `multer`, `file-type`, `express`/`qs`/`lodash` and `react-router-dom` are all runtime. The NestJS-mediated ones need semver-major bumps of `@nestjs/platform-express` + `@nestjs/swagger`, so plan it rather than running `npm audit fix`.
3. **Error monitoring** (no runtime observability).
4. **Deploy `dev → main`** — merging **auto-deploys**: `deploy.yml` fires on push to `main`, runs the build/test gate, then SSHes to EC2 and runs `scripts/deploy.sh`. ⚠️ **Migrations do NOT run by default.** On a push-triggered deploy the repo *variable* `RUN_MIGRATIONS` decides, and it defaults to `false` — so merging with it unset ships code for schema **12–20** that never arrived, against a prod DB still at #11. Either set `RUN_MIGRATIONS=true` (Settings → Secrets and variables → Actions → Variables; safe as a standing default, `migration:run` is a no-op when nothing is pending) or deploy manually from the Actions tab with the checkbox ticked. Also needs the prod env set (incl. optional `SLA_SWEEP_ENABLED`/`DIGEST_ENABLED`; translation stays off unless `TRANSLATE_DRIVER` is set) and the `SSH_KNOWN_HOSTS` secret pinned, or the deploy job fails closed. The `tokenVersion` change logs every staff user out once. → [[Configuration and Env]].
5. **AI triage (Plan 05)** — user-deferred. → [[Plan 05 - AI Triage Pluggable and Free]].
6. **Email-to-issue intake** — **do LAST** (user directive); needs a mail-provider decision.
- Decision-gated security: Redis throttler (M8), disk-streaming uploads (M6). Webhook SSRF: redirects are now refused and CGNAT blocked (2026-07-24); the remaining gap is DNS-rebinding (a public name resolving to a private IP) — hostname-string check only, admin-gated.

## Dev-environment gotchas (bit us on 2026-07-22)
- **`npm run seed` is idempotent by existence, not by content.** It only creates missing rows. A `admin@cimp.dev` row created some other way (2026-07-14, password-less and role-less) meant the whole Playwright suite failed at login. It now *repairs* `portal-a`'s hand-off secret and status, but staff rows are still create-only — if a staff login misbehaves, check `password_hash IS NOT NULL` and that the ADMIN grant exists before debugging code.
- **Never rotate `portal-a`'s hand-off secret in dev.** The Playwright helpers mint reporter tokens against the hard-coded `dev-secret-portal-a`; rotating breaks every reporter flow until `npm run seed` puts it back.
- FAFICS squats :3000/:3001 and its Postgres owns :5432 — **CIMP's Postgres is `cimp-pg` on :5433**. Run the backend with `PORT=<free> npm run start:dev`, and point Vite at it with `VITE_PROXY_TARGET`.
- **The documented DB port is wrong and will waste your time (found 2026-07-28).** `docker-compose.yml` maps `5432:5432` and both `.env.example` and `CLAUDE.md` say `:5432`, but the working `.env` uses `DB_PORT=5433`. Since FAFICS holds 5432, the documented `docker compose up -d postgres` cannot bind — the evidence is `cimp-postgres-1` sitting in **Created**, never started. **Start the real one: `docker start cimp-pg`.** Fixing the three files to 5433 is on the next-session list.
- **Playwright browsers are NOT installed by `npm install` (found 2026-07-28).** `~/AppData/Local/ms-playwright` did not exist, which is why every browser test died instantly and the suite appeared to "time out". Run **`npx playwright install chromium`** once. Nothing in the README or CLAUDE.md mentions this.
- **Reliable Playwright recipe** (the `webServer` auto-start is flaky here): `docker start cimp-pg`, then start the API (`PORT=3972 SLA_SWEEP_ENABLED=false DIGEST_ENABLED=false npm run start:dev`) and Vite (`cd frontend && VITE_PROXY_TARGET=http://localhost:3972 npm run dev -- --port 5199 --strictPort`) yourself, wait for both, then `cd frontend && npx playwright test`. `reuseExistingServer: true` picks them up. **Never run `npm run test:e2e` twice concurrently** — the two runs race for :3972/:5199 and *both* fail with `ERR_CONNECTION_REFUSED`, which looks exactly like a broken app.

## Gotchas before merging `dev → main` (which deploys)
- Config is **fail-closed**: prod needs `NODE_ENV=production`, `DB_SYNCHRONIZE=false`, explicit `CORS_ORIGINS`, `JWT_SECRET`≥32, `SCAN_DRIVER=clamav` (or `ALLOW_UNSCANNED_UPLOADS=true`) — or it won't boot. → [[Configuration and Env]].
- The `tokenVersion` change **logs out all staff once** on deploy (re-login).
- `search_vector` FTS only works where the `AddIssueSearchVector` migration ran (prod); under dev `synchronize` description-search returns nothing (reference search still works).

## Working conventions
- Commit to `dev` (or a feature branch → `dev`), never straight to `main`. **No `Co-Authored-By: Claude` trailer.**
- New feature = follow the pattern in [[Features - Shipped]]: entity + migration + scoped service + controller + spec, then wire in `app.module.ts`, then UI in `IssueExtras.tsx`/relevant page.
- Reuse enums (`src/common/enums.ts`), the `PlatformAccessGuard`, and `ScopeService`. Keep everything tenant-isolated (same-platform checks).

## This vault lives inside the git repo (committed)
`D:\CIMP\CIMP\vault` is under the repo root and is **committed** as shared team docs. Obsidian's per-machine UI state (`vault/.obsidian/workspace.json`, `graph.json`, `cache/`) is gitignored; the shared config (`app.json`, `appearance.json`, `core-plugins.json`) is tracked. Keep it current with `/doc-sync` after a change (see [[LLM Guide]]).

Related: [[CIMP - Home]] · [[Decisions and Glossary]] · [[Deployment, CI-CD and Dev Workflow]]
